#!/usr/bin/env bash
case "$1" in
  "up")
    docker compose up --build "${@:2}"
    ;;
  "cli")
    docker compose run --rm devcontainer bash -c "cd app && bash"
    ;;
  *)
    echo "fq"
    echo "  up   run the project"
    echo "  cli  run a command in the cli"  
    ;;
esac